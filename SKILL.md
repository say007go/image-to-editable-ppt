---
name: image-to-editable-ppt
display_name: 图片转可编辑PPT
display_name_en: Image to Editable PowerPoint
description_zh: 将幻灯片图片或 PDF 页面高保真逆向重构为原生可编辑 PPTX：文字、容器、图形、路径、图标和表格按独立对象拆解，支持局部图片例外、画布与配色变换、叠加校准和结构验收。
description_en: Reconstruct slide images or PDF pages into high-fidelity, natively editable PPTX files by decomposing text, containers, shapes, paths, icons, and tables into independent objects, with controlled image exceptions, canvas and palette transformations, overlay calibration, and structural QA.
description: Convert slide images or PDF pages into high-fidelity, native editable PowerPoint files with independent objects, controlled visual transformations, overlay calibration, and structural verification.
version: 1.0.0
author: 圣婴
---

# 图片转可编辑 PPT

## 技能使命

把一页幻灯片图片或 PDF 页面拆解为可编辑的 PowerPoint 对象，并在视觉上尽可能贴近输入素材。默认任务是保真重构，不是重新设计、改写文案或套用新的模板。

## 默认前提

本技能默认由具备图像理解能力的多模态模型执行。模型必须实际查看输入页面及必要的局部放大图，以视觉证据判断文字、坐标、颜色、层级和图形轮廓；不能只根据文件名、OCR 摘要或经验猜测版式。

运行环境还必须具备原生 PPTX 写入、渲染和基本结构检查能力。若无法查看输入页面、生成 PPTX 或渲染复核，不得声称完成高保真转换，应明确说明缺失的验证环节。

## 跨页面独立原则

每张输入页面必须从新的对象清单和新的几何判断开始。其他页面、历史版本、模板、旧的构建代码或曾经通过验收的局部实现，都不是当前页面的视觉证据。

可以复用工作流、适配器接口、字体处理、资源许可证记录和已经验证的基础能力；不得默认复用当前页面未证明的对象数量、坐标、几何关系、样式、图标选择、层级或路径参数。只有当用户明确要求统一风格，或当前页面直接呈现出相同结构时，才可复用相应的样式或构件，并重新以当前页面的视觉证据校准几何。

## 适用范围

当用户提出以下需求时使用本技能：

- 将幻灯片截图、PNG、JPG 或 PDF 页面转换为可编辑 PPTX
- 让图片中的文字可以双击修改
- 让色块、卡片、路径性/方向性图形、边框和分割线可以单独选中调整
- 在保留版式的基础上改用企业品牌色或调整画布比例
- 对已经生成的 PPTX 进行可编辑性、文字保真和视觉一致性检查

以下需求不属于本技能的默认范围：

- 从零创作一套新的演示文稿
- 对输入页面进行内容重写、逻辑重组或重新设计
- 只把整张图片放入 PPTX 并声称已经完成可编辑转换

## 不可逾越的优先级

发生冲突时，按以下顺序判断：

1. 用户明确提出的目标、比例、配色和交付要求
2. 输入图片或 PDF 中可观察、可核对的事实
3. [核心重构协议](references/core-reconstruction-protocol.md)
4. 本技能的默认规则
5. 当前运行环境的实现方式

不要用运行环境的限制去改变输入内容；如果环境无法生成或验证 PPTX，应明确说明能力缺口，不要伪造完成状态。

## 默认策略与变换模块

默认关闭所有变换模块，保留输入素材的版式、配色、字体和层级；基础重构遵循核心提示词规定的 16:9 宽屏默认画布。若输入页面本身为 4:3 或其他特殊比例，则按核心提示词通过自适应留白居中，严禁拉伸变形。

- 用户提出“换成企业色”“使用品牌 VI”时，启用品牌配色变换模块。
- 用户提出“改成 16:9”“适配 4:3 投影”时，启用画布比例变换模块。
- 用户同时提出两类要求时，先完成基础重构，再依次进行画布比例变换和品牌配色变换。
- 用户没有明确提出时，不主动替换颜色或改为其他目标比例；未指定特殊比例时，采用核心提示词规定的 16:9 默认画布。
- 用户要求变换但没有提供关键参数时，只询问缺失的参数，不自行编造品牌色或目标比例。

变换模块的详细规则见 [变换模块](references/transformation-modules.md)。

## 通用形态与图标的默认决策

图形重构遵循“视觉证据优先、形态拟合优先、对象独立”的选择规则：

1. 先从当前页面的可见区域、边缘、遮挡关系和负空间中分离独立视觉对象，再决定它的几何表达；不得先按名称或常见模板套用形状。
2. 每个对象至少记录可见掩膜、闭合轮廓、开放路径/中心线、控制点、填充模型、描边模型、端部或局部构件、z-order 和不确定项。只有当前页面的拟合结果证明对象之间存在共享参数时，才复用中心、半径、旋转、边界框或其他关系。
3. 按拟合结果选择最接近的原生预设、稳定的原生基础形状组合、Custom Geometry 或独立矢量对象；预设只在目标尺寸和当前页面局部渲染探针通过后使用。语义名称不能替代可见几何证据。
4. 对细长描边对象，优先保留开放路径与描边属性；对面状对象，优先保留闭合轮廓与填充属性；对复合对象，按可见子轮廓和层级拆成独立构件。端部尖点、缺口、圆角或其他局部形态应从当前轮廓中拟合，不凭语义补画。
5. 需要保持特殊轮廓时，将确认过的矢量路径转为独立原生 Custom Geometry；若当前环境不能可靠转换，可保留为独立矢量对象，但不能把主要结构合并为整页 SVG 或位图。
6. 标准图标从候选图标库中选择一套许可证清晰、风格匹配的独立 SVG 矢量资源；不能用圆、线和矩形临时拼接标准图标。

默认不要求用户在开始时选择图标库。候选资源固定为 IconPark、Remix Icon 和 Lucide；执行时根据当前环境的资源可达性、输入页面的线性/填充风格、精确语义覆盖和许可证可追溯性，选择一套最合适的图标家族。同一页原则上只使用一个家族，除非输入页面本身明确混用或用户明确指定。

图标官网只是检索入口，不作为生成任务的在线硬依赖。优先使用已安装的固定版本、本地缓存或随任务保存的 SVG；需要联网时先做资源探针，失败则切换候选库并记录回退原因。任何图标都必须保留来源、版本、图标标识、许可证和内容哈希，不得因资源暂时不可达而静默降级为低分辨率 PNG。

局部 SVG 图标与整页图片不是同一类对象。默认要求每个图标作为独立矢量对象移动、缩放和按资源能力换色，但不声称 SVG 内部每条路径都能单独选中。若用户明确要求路径级编辑，将可可靠转换的路径拆为原生 Shape；无法可靠转换时必须说明限制。

详细的预设形状、通用路径构件、图标来源、编辑级别和适配器要求见 [通用形态与图标资源协议](references/shape-and-icon-protocol.md)。

## 标准工作流（四步一循环）

下面四步对应核心提示词的五个 Step：把“对象计划＋编译”合并为装配，把“视觉比对＋修复”合并为一个验证循环，不增加额外运行模式。

### 1. 锚定：输入与视觉解构

识别图片、单页 PDF 或多页 PDF，记录页数、尺寸、长宽比和清晰度；多页按页处理，可共享已确认的字体映射、命名约定和用户授权变换，但每页的对象数量和几何关系必须重新判断。多模态模型必须实际查看页面和必要的局部放大图。

建立一份对象清单或等效场景图，至少记录 `id`、类型、内容、边界框、样式、z-order、父组、可编辑级别和不确定项。逐项识别文字、背景、色块、卡片、边框、线条、路径性/方向性图形、图标、表格、图片和层级关系；记录 X/Y、宽高、颜色、透明度、线宽、字号、字重、对齐方式和来源。

文字逐字记录标点、空格、大小写、数字单位和显式换行；按图形实际可见形态记录掩膜、闭合轮廓、开放路径、中心线、端点、控制点、填充/描边和组合构件。任何共享的几何关系只在输入页面的拟合结果明确支持时使用。无法确认的细节标为不确定项，不凭空补齐。

默认设置：目标画布采用 16:9；若输入页为 4:3 或特殊比例且用户未指定其他目标，则按输入长宽比自适应留白；保留输入配色、层级和文字；文字逐字保真；图形和容器独立；图片只保留允许的局部视觉资产。

### 2. 装配：对象选择与原生编译

按以下顺序决定表达方式：通过探针的预设形状 → 组合原生形状 → Custom Geometry → 独立矢量对象；真正不可拆解的照片或三维视觉才使用局部图片。

- 文字：TextBox 或 Text Run；
- 容器、色块、边框和分割线：独立 Shape；
- 图形对象：根据当前页面拟合出的轮廓、路径和局部构件选择匹配的原生表达；需要端部构件的对象将主体与端部分开保留，不能因为语义或邻近布局改变其实际形态；
- 标准图标：从候选库中选择一套许可证清晰且与当前页面风格匹配的 SVG 图标库，每个图标独立；
- 表格和数据：使用可编辑表格、文本和图形对象。

读取并执行 [核心重构协议](references/core-reconstruction-protocol.md)、[通用形态与图标资源协议](references/shape-and-icon-protocol.md) 和 [可编辑性契约](references/editability-contract.md)，使用当前环境可用的原生 PPTX 写入能力完成编译。

对于可能与文字、标签、虚线或相邻容器相交的主图形，必须先划定当前页面可见的保护区，再确定主体路径和局部构件；标签默认透明填充并放入实际留白，不用白色文本框遮挡几何错误。

用户明确要求换色或改比例时，先保留基础重构版本作为基线，再执行 [变换模块](references/transformation-modules.md)。

### 3. 校准：渲染、叠加与定向修复

将 PPTX 渲染到与输入页面相同的画布，生成透明叠加图、差异图和统计结果。先看整体锚点、容器边界和主图形轮廓，再查看当前页面中视觉密度最高、遮挡风险最高或几何最复杂的局部 ROI；ROI 必须由当前页面决定。

换色或字体替换时优先比较轮廓、边缘、对象框和安全区，不把整块颜色差异当作坐标错误。把偏差映射回对象清单，只提交已定位对象或对象组的坐标、尺寸、旋转、路径或内边距补丁，不凭差异图盲目平移整页。

第一轮处理画布、容器和主图形；第二轮处理局部几何、路径接缝、局部构件、图标和文字内边距；第三轮只处理仍影响使用的硬伤。每轮修复后重新渲染或结构检查，连续两轮无改善时停止重复尝试。

重点检查文字保真与可编辑性、对象遗漏与遮挡、路径和局部构件的连续性、图标一致性、页面溢出、文本截断、标签安全区、容器右/下内边距以及整体比例和留白。

详细规则见 [叠加比对与坐标校准协议](references/overlay-calibration-protocol.md)、[缺陷修复协议](references/repair-registry.md) 和 [验证与验收](references/verification-and-acceptance.md)。

### 4. 交付：结构验收与披露

确认文件可打开、文字和主要对象可独立操作、渲染结果没有影响使用的明显错位、遮挡、截断或遗漏，并如实区分对象级可编辑与路径级可编辑。交付可打开的 `.pptx` 文件，默认附带不超过三行的近似替代说明。

## 原生可编辑性底线

- 禁止将整页输入图片作为 PPTX 背景或唯一内容。
- 禁止将文字栅格化、转曲或嵌入图片。
- 文本、容器、边框、路径性/方向性图形和分割线必须能够独立选中；复合图形的主体路径与局部构件应保持独立，标准图标至少作为独立矢量对象选中。
- 复合文本中不同字号、颜色或字重的部分必须拆分为独立 Run 或文本框。
- 虚线边框使用无填充几何 Shape 配合原生破折线描边，不使用离散点阵拼接。
- 设置中文与西文字体时，同时处理 eastAsia 与 latin 字体属性。
- 所有 TextBox 默认设置 `word_wrap = false`，并预留 30~100pt 动态右侧内边距；多行文本通过显式换行、拆分文本框或调整文本框高度保持输入中的断行，最终以不截断、不溢出为验收条件。
- 标签默认使用透明填充并放入预留留白；只有输入页面明确显示白色底板或光晕时，才允许使用独立白色对象。
- 复杂照片或产品渲染图可以作为局部图片，但不得包含可被重建的文字、表格或主要结构。
- 局部开源 SVG 图标可以作为独立矢量对象使用，但不得把整页、主要结构、文字或表格降级为 SVG。

## 参考文件路由

- 每次完整转换：读取 [核心重构协议](references/core-reconstruction-protocol.md)。
- 页面含标准图形、复杂路径或图标：读取 [通用形态与图标资源协议](references/shape-and-icon-protocol.md)。
- 需要改比例或换品牌色：读取 [变换模块](references/transformation-modules.md)。
- 发现转换缺陷或用户报告异常：读取 [缺陷修复协议](references/repair-registry.md)。
- 需要判断是否合格：读取 [验证与验收](references/verification-and-acceptance.md)。
- 需要通过源图与渲染图调整坐标：读取 [叠加比对与坐标校准协议](references/overlay-calibration-protocol.md)。
- 需要适配不同 AI 或文件生成环境：读取 [运行环境适配契约](references/runtime-adapter-contract.md)。
